# Finding ledger

Status values: `OPEN`, `REPRODUCED`, `FIXED_PENDING_REVIEW`, `CLOSED`, `DEFERRED`, `OUTDATED`.

| ID | Severity | Owner layer | Finding | Initial status | Required closure evidence |
|---|---|---|---|---|---|
| XF-001 | P0 | MCP reliability | A lost create response without Provider ID previously blocked replay but could not converge; the current candidate now has a five-minute, same-key, one-claim native-idempotency recovery path. | FIXED_PENDING_REVIEW | Postgres integration, process/fault evidence, one Provider object, exact readback, and real test-tenant recovery evidence. |
| XF-002 | P0 | Agent2 configuration/OAuth | Latest retained UAT showed an Agent2 client-ID mismatch and earlier host routing drift. Unauthenticated preflight on 2026-08-18 now establishes a stronger blocker: the deployed MCP is **not** the accepted candidate. Its `toolsetHash`, compiler, provider contract and public tool profile match, but `requiredMigration` is 039 against the candidate's 040 (`040_xero_native_idempotency_recovery_claim.sql`), and `attestationHash` and `acceptanceSourceSha256` both differ — so Agent2 runs a build predating this round's headline P0 fix. It is also `writeMode: READ_ONLY` with the process write gate closed and `standingDelegationsConfigSha256 = sha256("[]")`, i.e. zero standing delegations. | REPRODUCED — G5 NO-GO | Deploy the frozen candidate, migrate to 040, configure a standing delegation for the intended action, and open the write gate for exactly one installation and tenant. Then re-run this preflight and require every identity field to match before any online write. Evidence: `agent2-preflight/AGENT2-DEPLOYMENT-PREFLIGHT.md`. |
| XF-003 | P1 | OAuth scopes | The Broker grant now maps `xero.draft.write` to Invoice plus Contact write only; unused Manual Journal and settings/item write scopes are no longer selected. Existing authorizations may retain old grants until re-authorized. | FIXED_PENDING_REVIEW | Live re-authorization and granted-scope evidence for Agent2, plus exact Case execution regression. |
| XF-004 | P1 | Skill/MCP architecture | Skill, Xero profile, Agent instructions and capability contract now use one authorization model: exact per-transaction approval or current platform-bound standing delegation, never both; controlled same-key native recovery is runtime-only. | FIXED_PENDING_REVIEW | Deployment-equivalent bundle hash/load evidence and Agent behavior regression without a per-item confirmation request. |
| XF-005 | P2 | Provider scalability | Duplicate/history scans are bounded and fail-closed, not literally unbounded; current upper limits remain large and have no single-Case Provider-call budget. | DEFERRED | Configured page/call/row budget or indexed follow-up; no production-scale claim in release evidence. |
| XF-006 | P0 | Release identity | The acceptance-critical candidate is a large dirty worktree with stale status documents. | PARTIALLY_CLOSED | Frozen source selection and a complete 566-file acceptance-source manifest now exist and reproduce from two independent code paths: `CANDIDATE-FREEZE.json` and the Gate L driver's own snapshot step (`gate-l/source-snapshot.manifest.json`), both yielding `dbf0ce4da0818e217a6dc76a432b11d3e35e5be6a0ae4360781f86498395fd74`. Still open: no artifact or image digest exists, because the release-bundle and OCI steps have not been run, so there is nothing yet to hold constant across local and Agent2. |
| XF-007 | P1 | Agent token efficiency / tool UX | Run 01 used 350,817 input tokens and needed three prepare attempts after two schema-shape errors. The narrow 5-tool model-visible Accounting Case profile over the unchanged 28-tool backend removed schema repair entirely: main chain 06 completed `pin -> organisation read -> prepare -> execute` with zero repair retries. | PARTIALLY_CLOSED | Correctness half CLOSED by main chain 06 (zero schema repair, one write, receipt, exact readback). Cost half OPEN: input is 338,306 tokens because the now-mandatory Skill and reference reads add back what the narrow profile saved (run 04 was 201,501 without them). Reducing mounted-instruction bytes without dropping required business capability is deferred to a separate work package. |
| XF-008 | P1 | PostgreSQL test isolation | Re-running the required integration suite on the same disposable database exposed a fixed CONTACT object ID left by the prior run, causing one unique-index conflict despite a fresh per-run Case suffix. | CLOSED | Fixed with a per-suite Provider object UUID; targeted 26 tests passed on the already-dirty database, followed by two consecutive complete 16-file/110-test required-gate passes on that same database. |
| XF-009 | P1 | Crash-evidence fixture | The real process-crash generator had drifted from the current server-owned CoA and exact-readback contract: its Provider exposed one account and omitted current `accountId`/`invoiceNumber` readback fields. | CLOSED | Provider master data now comes from the exact three-category CoA profile and returns the current exact-readback shape; all four real SIGKILL/restart scenarios passed and raw evidence independently recomputed. |
| XF-010 | P1 | Crash-evidence Postgres readiness | The generator's socket-default `pg_isready` could observe the official image's temporary bootstrap postmaster, then race its shutdown during `createdb`. | CLOSED | Readiness now probes `127.0.0.1` TCP; the final run created all four databases, completed all scenarios, and automatically removed its container. |
| XF-011 | P2 | Experimental review governance | The non-runtime 18-requirement/90-claim mutation-review artifact reuses 57 cross-claim probe fingerprints; its review fixtures also expose one oversized binary semantic unit, fixture paths outside the repository, missing fixture TypeScript identity and validation-order drift. Repairing the governance framework does not add evidence to the current Xero DRAFT/receipt/readback closure. | DEFERRED | Give this framework a separate budget and deterministic per-claim probe generator; until then it remains explicitly NOT PASS and is excluded from the Agent2 test-environment release Gate, while runtime and business audits remain mandatory. |
| XF-012 | P1 | OAuth/runtime authorization | A write client could refresh or recover a raced refresh token and continue without revalidating the scopes returned by the Broker. The client manager now rechecks current Broker scopes before any Provider action and fail-closes as `SCOPE_MISSING` with `providerMutationPossible=false`. | FIXED_PENDING_REVIEW | Focused and whole-runtime regression plus real re-authorization with the final granted scopes before the test-tenant write. |
| XF-013 | P1 | Provider idempotency boundary | The durable mutation request ID was not previously proven to be the exact idempotency key passed to every raw Xero create adapter. Provider authorization now requires a non-empty key equal to `mutationRequestId`; all ten raw adapters pass the actual SDK key and persistence records that same key. | FIXED_PENDING_REVIEW | Focused permit/adapter assertions, PostgreSQL and crash regression, then one real-Xero duplicate replay proving zero additional object. |
| XF-014 | P1 | Durable write evidence | The Provider draft-write evidence callback was optional, allowing a caller to cross the write boundary without a durable recorder. It is now mandatory and checked before client acquisition, token refresh, permit consumption or Provider I/O. | FIXED_PENDING_REVIEW | Focused fail-closed assertions plus one real-Xero write with durable receipt and same-ID readback evidence. |
| XF-015 | P1 | Local deployment simulation | The isolated harness disabled every filesystem-reading tool, so the Agent could not load the mandatory filesystem-backed Skill and correctly stopped before MCP use. The harness now permits audited read-only access to exactly the ephemeral mounted Skill files. | FIXED_PENDING_REVIEW | Main chain 06 read all four mounted Skill/reference files, issued no other shell command, and then ran the retry-free four-call Xero sequence with one write, receipt and exact readback. See `local-agent/MAIN-CHAIN-06-RESULT.md`. Closure still needs the validated evidence document from XF-016. |
| XF-016 | P0 | Acceptance evidence generator | The main chain 06 raw evidence (`local-agent-final-after-skill-loader.raw/`) exists, replays, and is internally consistent, but the validated, source-bound `…​.json` evidence document was never emitted: the generator run stopped at a Codex account usage limit before the final summary step. Gate L consumes the validated document, not the raw directory, so Gate L cannot currently be run to completion. This is a quota/operational blocker, not a defect in the candidate. | OPEN | Restore Codex account quota, then re-run `npm run evidence:local-agent --evidence <path>` against the frozen candidate so a schema-valid, hash-bound local-agent evidence document exists, and feed it to `npm run release:local:gate`. |
| XF-017 | P1 | Acceptance mechanism reliability | `startTreeMutationMonitor` reported mutations that never happened: on macOS the FSEvents stream behind `fs.watch(root,{recursive:true})` delivers a backlog of changes predating the watcher, so the `IMMUTABLE_SNAPSHOT` monitor observed the `makeTreeReadOnly` chmods issued just before it was created. Idle snapshot, before fix: 3 spurious `change` events within 50-200 ms in 12 of 12 trials; `tests/local-acceptance-mechanism.test.ts` failed 1-3 of 24 tests run to run and Gate L was non-repeatable. | FIXED_PENDING_REVIEW | Fixed by deterministic watcher arming with a sentinel watermark at the watched root; pre-arm backlog is discarded, an unarmed watcher reports nothing and defers to the mandatory inode/ctime/mtime guard, and both monitors' armed state is recorded in the snapshot manifest. Verified: 0/8 idle false positives, 8/8 genuine A-to-B-to-A detections, 24/24 tests passing over 10 consecutive runs, 5 files/45 tests adjacent regression PASS, typecheck PASS, arming cost 14-70 ms. Needs independent reviewer sign-off. |
| XF-018 | P1 | Repository exposure | The GitHub remote `johnw-max/xero-mcp` is public and already carries a real Xero Developer App client ID, the production hostname `mcp.jiayuanwang.xyz`, and internal OAuth client identifiers, in previously pushed commits. No client secret was found anywhere in the worktree or in full history. This predates the current work and is not added by the pending diff. | ACCEPTED_BY_DECISION | User decision, 2026-08-18: accepted as low risk. A client ID without its secret is not directly abusable, and no secret leaked. The Xero app registration is **not** being rotated. Standing condition: redact client IDs and production hostnames from any new document before it is committed. Revisit immediately if a client secret, refresh token, or tenant credential is ever found in the repository. |
| XF-019 | P0 | Acceptance scope vs harness capability | The round's Definition of Done fixes the test object as **one Supplier Bill DRAFT**, but the local deployment-equivalent harness cannot execute a Supplier Bill at all. In `harness/runners/run-p0-accounting-case.ts` the synthetic provider's `createDraftSupplierBill` and `getSupplierBill` both call `#unexpected(...)` and throw, and the standing-delegation fixture grants only `customer_invoice.create_draft`. A SUPPLIER_BILL Case therefore fails at `STANDING_DELEGATION_ACTION_MISMATCH` before reaching the provider. Consequently local main chains 01-06 all tested a **customer invoice**, not the object this round is about, and G6 on Agent2 would have been the first end-to-end exercise of the Supplier Bill path — violating the plan's own rule that Agent2 only repeats a chain already passed locally. The product runtime does support the action (`accountingCaseCompiler.ts:152`, `xeroAutonomousActions.ts`, `xeroProvider.ts:1404`, `xeroProviderWritePermit.ts:29`); the gap is harness-only. | FIXED_PENDING_REVIEW | Fixed in `harness/runners/run-p0-accounting-case.ts` only (no `src/`, no test change): implemented a real `createDraftSupplierBill`/`getSupplierBill` pair mirroring the sales-invoice path's write gate, permit consumption, idempotency and exact readback, using a distinct ACCPAY object ID; add `supplier_bill.create_draft` to both standing-delegation fixtures. Then re-run the natural-language main chain on a Supplier Bill and record it as the object the Definition of Done actually names. |
| XF-020 | P2 | Test environment access | The in-app browser cannot render the Agent2 SPA: every same-origin `/assets/*.js` and `/assets/*.css` request fails with `net::ERR_BLOCKED_BY_CLIENT`, reproducibly, leaving only the loading skeleton. No Chrome instance is connected for the alternative browser surface. Conversational UI testing on Agent2 is therefore not currently possible from this session. | OPEN | Connect the Claude in Chrome extension, which also carries the existing logged-in session and so avoids any credential entry. Not the binding constraint while XF-002 stands: no valid G6 result is obtainable from the current deployment regardless of browser access. |
| XF-021 | P0 | Plan vs product contract | The round's Definition of Done fixes the test object as one **Supplier Bill DRAFT under Standing Delegation**, but the product refuses to autonomously create it. `xeroDocumentCoordinateAuthority` (`src/policy/xeroBusinessCoordinateAuthority.ts:26-49`) classifies SUPPLIER_BILL as `NON_UNIQUE_EXCLUSIVE_WRITER` — faithful to Xero, where an ACCPAY bill number is not unique per tenant, unlike an ACCREC invoice number. For such routes `xeroAccountingCaseService.ts:846-874` requires `writerAuthority.mode === "VERIFIED_FIRM_GOVERNANCE"` with `governanceAuthorityActive === true`; without it the execute fails closed with `PROVIDER_BUSINESS_COORDINATE_ATOMICITY_UNPROVEN` and `provider_mutation_possible: false`. Neither environment has that authority: the local harness configures none, and the deployed instance reports `firmGovernance.status: NOT_REQUIRED`, `authorityCount: 0`. Reproduced live — see `LOCAL-CONVERSATION-RESULTS.md` §4. The identical Case on the CUSTOMER_INVOICE route succeeded with object ID, receipt and exact readback, so the refusal is route-specific and correct. **The product is behaving correctly; the plan selected an object it also declared out of scope to authorise.** | OPEN — USER DECISION | Pick one: (a) configure verified firm-governance exclusive-writer authority in both environments and keep the Supplier Bill; (b) change the round's fixed object to a Customer Invoice with a formal document number, which is what every passing run has actually exercised; or (c) accept Supplier Bill at `REVIEW_REQUIRED` and move its write to an explicit human-approval path. Until this is decided the round's Definition of Done cannot be met as written. |
| XF-022 | P1 | Duplicate protection evidence | Definition-of-Done item 8 (repetition/retry/session recovery must not create a second document) had never been demonstrated by any run. | CLOSED | Demonstrated 2026-08-18 in `conversations/local/session-b-route-and-duplicate/`: after one successful write, a same-`request_id` replay, a different-`request_id` re-execution, and a brand-new Case restating identical document facts all failed to produce a second object — the last refused at prepare with `ACCOUNTING_CASE_BUSINESS_COORDINATE_ALREADY_RESERVED`. Server audit records `provider_write_count: 1` across all three execute calls. Synthetic provider boundary only. |
