# OAuth release closeout results

## Verdict

PARTIAL / NO PUSH YET — the current 0.4.0-rc.1 product candidate is green locally and on a fresh PostgreSQL database. The selected validation origin is healthy, but it still serves 0.3.1, so the fixed OAuth return path has not been proven in Agent2 or Work. Formal independent-review evidence also remains OPEN. No admission bypass was used.

## Evidence summary

`XERO_WRITE_ENABLED=false` remained a hard invariant. Current candidate evidence:

- OAuth focused tests: 164/164 PASS, including Work direct-302 and Agent2 manual-return separation.
- Post-pause exact-snapshot product regression excluding the two intentionally unresolved reviewer/traceability files: 1,329 ordinary tests PASS plus 15 loopback/transport tests PASS outside the sandbox (1,344 total), with no product assertion failure.
- Current affected-helper regression: 24 files / 395 tests PASS.
- Fresh disposable PostgreSQL required suite: 15 files / 107 tests PASS, migrations 001–039.
- Post-pause exact `/tmp` snapshot of the current bytes completed typecheck, build and the production static contract successfully. The snapshot avoids treating iCloud hydration latency as a source failure. `git diff --check`: PASS.
- Full dependency audit: 0 vulnerabilities after lockfile-only nanoid 3.3.18 patch.
- Diagnostic deterministic source bundle: build and independent verify PASS; 192 files, zero secret/legacy-domain/forbidden-path findings.
- Current-byte deterministic bundle was regenerated after product closeout and is byte-identical to the runtime-smoked candidate: acceptance source `440dfdef916969cacfbe313d5e445b98f70d5c2a6bdd3693ed9f9a7fdd6b6f4b`, source archive `e1e88ec2e88b97755e2c0798d36cd9f2c9dae37e47d6e5c0ca7e81067ba5da92`, semantic build identity `96dacfd3bd55037ee1bb337996a989754f82e0ae86a89e544bbb37cdbe6607ec`.
- OCI build and isolated runtime smoke PASS for artifact `bf4bf0cbd99728681e98d95d979e5e0b57781e9eb2c8b8d9618d9a269c378596` / manifest `sha256:43c1dba6f0f43eaa08bf6fa62fa8201b17a56c0bcb2289af7600bf04f9f8f807`: fresh migration through 039, read-only readiness, anonymous MCP 401 and malformed MCP 400. This is a locally verified candidate OCI, not a formally Gate-accepted production-write artifact.
- Traceability: schema 4.0, 18 requirements / 90 claims, all OPEN; a fresh approved-catalog validation still FAILS repeated cross-claim probe fingerprints. This is an honest release blocker.
- Independent-review authority: a fresh valid-shape invocation of the repository-local host-attestation boundary exits 78 with `LOCAL_EVIDENCE_UNTRUSTED` because no out-of-repository signed parent attestor exists.
- Three-role bounded closeout review (business/product, technical architecture, release governance): all three independently returned NO-GO for autonomous production writes. They agreed that OAuth/OWASP and the core Accounting Case implementation are substantially present, but external firm-governance authority, current traceability closure, signed host review provenance and candidate-version live UAT remain hard release gates.

## Findings by owner

- MCP/OAuth: the candidate fixes the real MCP-side exposure by separating missing-resource compatibility from manual return. Work is excluded from manual return and receives a direct 302; Agent2 keeps its intended manual-return flow.
- Work/LibreChat: the prior `/login?redirect=false&error=auth_failed` terminal URL is generated by LibreChat global OpenID/login callback handling, not by the MCP OAuth callback route. The candidate can remove the known MCP trigger but cannot repair Work session/login code in this repository.
- Deployment ownership: the development team deploys its downstream copy at `xero.zcloak.ai`. This acceptance run deliberately uses `mcp.jiayuanwang.xyz` as the repository-owner validation origin; no claim is made about the downstream deployment.
- Live validation origin: `healthz` and `readyz` returned 200 for deployed version 0.3.1; authorization-server and protected-resource metadata returned 200; an unauthenticated MCP initialize returned the correct 401 challenge; Work and Agent2 CORS preflights returned 204 with their exact origins. This proves the existing baseline is reachable, not that the local 0.4.0-rc.1 OAuth fix is live.
- Hetzner: server `offbeatlabs` is running and port 22 accepts TCP, but SSH closes during key exchange before authentication even with the matching configured key; the IPv6 allocation is not reachable from this workstation. No server mutation, reboot, rescue action, deployment or cutover was performed.
- Xero: the prior video reached Xero consent and MCP organisation confirmation; no current-run live OAuth claim is made.

## Evidence boundary

No Git commit, push, deployment, cutover, or candidate-version live OAuth result is claimed. The disposable PostgreSQL container created for this run was removed after testing; its test data is not recoverable. Other containers were not touched. The temporary `/tmp` build snapshot contains only diagnostic build material and is removed at closeout.
