# Xero Accounting Agent MCP 产品需求文档

版本：V1 Draft  
日期：2026-08-05  
阶段：从单账套受控 Demo 升级为多用户会计工作连接层

## 1. Summary

我们要建设一套面向会计人员的 Xero MCP，让用户能够在 Work 中把自己的 Xero 账号连接给指定 Agent，并直接通过对话完成以下工作：

1. 查询 Xero 中已有的客户、供应商、发票、账单、余额和报表；
2. 把 Xero 历史数据与用户在 Work 中提供的发票、采购单、邮件、合同等材料放在一起分析；
3. 由 Agent 匹配联系人、科目、税码和跟踪维度，生成有依据的会计草稿；
4. 由会计人员确认高风险动作后写入 Xero，再从 Xero 精确回读结果；
5. 保留每个工具调用、来源材料、判断依据、人工审批和 Xero 对象 ID，便于复核。

Xero 始终是正式账本和会计事实来源。MCP Host 负责对话、材料、分析和人机协作；MCP 负责身份与账套隔离、Xero 能力调用、风险控制、防重复和审计。我们不新建总账，也不把 Google Drive 或 MCP 数据库当作账本。

**2026-08-05 UAT 决策：** 首个线上主测试宿主改为 Agent2（`agent2.zcloak.ai`），因为它更适合长材料和真实会计对话；连接协议保持 MCP Host 无关，Work 继续作为兼容宿主，不作为本轮主 UAT 环境。本文后续出现的 Work 可理解为原始产品称呼；新实现不得把 Work 域名、回调或身份字段硬编码进连接层。

当前基线已经验证单个 Trial 账套的 OAuth、会计环境读取、供应商账单草稿、人工审核、正式入账和回读。V1 的重点是把这个受控案例升级成会计人员能够自助使用、覆盖日常高频工作的产品能力。

## 2. Contacts

| 角色 | 职责 |
|---|---|
| 产品负责人 | 定义会计用户流程、版本范围、验收标准和风险边界 |
| MCP / 后端负责人 | 连接管理、工具契约、Xero Provider、权限、审计和部署 |
| Work / Agent 负责人 | 用户身份传递、Agent 配置、材料接入、审批交互和调用结果展示 |
| 会计领域负责人 | 会计规则、科目与税码匹配原则、异常案例和业务验收 |
| 安全负责人 | OAuth、令牌保护、租户隔离、最小权限、日志脱敏和威胁审查 |
| QA / UAT 负责人 | 契约测试、回归测试、线上真实用户旅程和证据归档 |

所有正式发布必须至少由产品、工程、安全和会计业务四方完成验收。Agent 生成的解释或建议不能替代会计人员的专业判断。

## 3. Background

### 3.1 用户问题

会计人员通常同时面对两类信息：

- Xero 内已有的客户、供应商、历史交易、应收应付和报表；
- Xero 外的新材料，例如 PDF 发票、收据、采购单、合同、邮件说明和表格。

传统操作需要用户在多个页面间检索、比对、判断、录入和复核。通用聊天 Agent 即使能读材料，如果不能安全读取和写入用户自己的 Xero，也只能给建议，不能形成闭环。相反，如果只把 Xero API 暴露给 Agent，又会产生错账、串账套、重复写入和越权操作风险。

### 3.2 当前已验证能力

- 一个受控身份连接一个 Xero Trial 组织；
- 读取组织、科目、税码、联系人、指定供应商账单和 Trial Balance；
- 创建一张 `DRAFT` 供应商账单；
- 人工审批后把同一 InvoiceID 变为 `AUTHORISED`；
- 写入防重复、精确回读、审计和写入总开关；
- Hetzner 上的远程 HTTPS MCP 已部署并接入 Work 做过受控验证。

### 3.3 当前缺口

- Work 用户身份目前映射到固定 Demo 身份，尚不能安全支持多个用户和团队；
- 一个用户只能隐式使用单个 Xero 组织，缺少多组织选择和客户账套切换；
- 历史读取只支持少量对象和精确 ID，不适合按客户、日期、状态做调查；
- 材料只有单一来源引用和哈希，尚未形成多材料、字段级证据与冲突处理；
- 只完成了供应商账单主链路，缺少销售发票、Credit Note、Payment 读取、附件和常用报表；
- 权限主要依赖全局写入开关，缺少用户、工作区、连接和动作级策略；
- Work 端还未形成普通会计用户可理解的连接、审批、回读与错误恢复体验。

## 4. Objective

### 4.1 目标

V1 要证明一名会计人员无需接触 Token、Tenant ID、InvoiceID 等技术参数，即可在 Work 中完成一条可信的日常流程：

> 连接自己的 Xero → 查询某客户或供应商的历史 → 提供多份新材料 → Agent 分析并说明依据 → 创建 Xero 草稿 → 用户确认 → 从 Xero 回读并解释结果。

### 4.2 成功标准

1. 用户可以自助连接、查看和断开自己的 Xero 连接；不同 Work 用户和工作区之间不能互相访问连接或数据。
2. Agent 可以按客户或供应商、日期、类型和状态查询应收/应付历史，不要求用户提供 Xero 内部 ID。
3. Agent 可以处理一个包含多份材料的工作包，标明每个关键字段来自哪里，发现冲突时停止并询问用户。
4. Agent 可以根据 Xero 现有数据提出联系人、科目、税码和跟踪维度匹配，并说明置信度与异常。
5. 草稿创建具有防重复、来源绑定、精确回读和用户可见操作凭证。
6. `AUTHORISE`、付款、作废、删除、Manual Journal 等高风险动作不能由 Agent 在无人确认时执行。
7. 六个真实会计 UAT 场景全部通过，且不存在串用户、串工作区、串 Xero 组织或重复写入。

### 4.3 非目标

- 不自建总账、银行流水系统、税务引擎、工资系统或文件存储平台；
- V1 不提供自动付款、自动报税、自动月结、自动审计结论；
- V1 不允许 Agent 自主 Authorise、Void、Delete、Manual Journal 或创建银行交易；
- 不提供任意 URL、任意 Xero Endpoint、任意 HTTP Header 或原始 `where` 条件的通用执行工具；
- 在 Xero V1 验收前不同时扩展 QuickBooks，以免两个不完整连接并行失控。

## 5. Market Segments

### 5.1 首要用户

为多家中小企业服务的会计人员、Bookkeeper 和外包财务团队。他们需要频繁在客户材料和不同 Xero 组织之间切换，对查询、录入效率和可追溯性要求高，但不能接受 Agent 直接完成不可逆操作。

### 5.2 次要用户

- 中小企业内部财务人员：处理应收应付、费用材料和管理报表；
- 会计事务所复核人员：查看历史、异常和证据，批准其他成员准备的草稿；
- 企业负责人：以只读方式询问经营和现金流情况，不直接执行会计写入。

### 5.3 暂不覆盖

大型企业复杂合并报表、强监管金融机构、完整审计底稿系统、工资与税务申报系统，以及要求无人值守自动付款的场景。

## 6. Value Propositions

### 6.1 对会计人员

- 用自然语言跨 Xero 历史和新材料检索，不再反复切换页面；
- 让 Agent 完成资料整理、历史比对、编码建议和草稿录入，把时间留给专业判断；
- 每项建议都能看到来源、匹配理由和 Xero 回读结果；
- 出现信息冲突、权限不足或结果不确定时明确停止，而不是悄悄猜测。

### 6.2 对会计事务所和企业

- 一个 Work 工作区内可管理多个成员和多个客户账套，同时保持连接隔离；
- 用统一策略控制哪些 Agent 可读、可起草、需审批或完全禁止；
- 用审计记录回答“谁在何时根据什么材料对哪个 Xero 对象做了什么”；
- 保留 Xero 作为唯一正式账本，降低迁移和双账风险。

### 6.3 对产品平台

- 形成可复用的会计连接、材料证据、审批和回读模型；
- 后续可在相同安全模型下扩展 QuickBooks，而不是为每个平台重做一套 Agent 工作流；
- MCP 工具保持业务语义清晰、输入有界，便于不同模型稳定调用。

## 7. Solution

### 7.1 产品流程

1. 用户在 Work 选择“连接 Xero”，通过 Xero 官方 OAuth 页面授权。
2. MCP 将连接绑定到可信的 Work 用户、工作区和所选 Xero 组织；Token 加密保存，永不返回给 Agent。
3. Agent 先读取组织、能力与权限，再按用户问题查询联系人、发票、账单和报表。
4. 用户把 PDF、图片、邮件、采购单或表格加入当前工作；Work 负责文件接收和文本/结构化提取。
5. Agent 形成 Source Bundle：列出材料、哈希、关键字段来源、冲突和缺失项。
6. Agent 从 Xero 读取候选联系人、科目、税码、跟踪维度和相关历史，生成匹配建议。
7. 对低风险动作，Agent 可直接读取、计算和起草；对高风险动作，MCP 返回人类可理解的审批内容。
8. 用户确认后，MCP 执行受控写入，并以同一 Xero 对象 ID 精确回读。
9. Work 向用户展示结果、状态、Xero 链接、来源摘要、审批人和审计编号。

### 7.2 系统职责

| 组件 | 主要职责 | 不负责 |
|---|---|---|
| Work | 用户/工作区身份、对话、材料上传、Agent 编排、审批体验 | 不保存正式账目，不直接持有 Xero Token |
| Agent | 理解任务、调用有界工具、分析历史、匹配和解释 | 不绕过权限，不自我审批，不直接调用任意 Xero API |
| Xero MCP | 连接、租户选择、工具契约、策略、防重复、回读、审计 | 不实现总账、OCR、税务判断引擎 |
| Xero | OAuth、会计对象、状态、报表和正式账本 | 不负责 Work 中的材料协作和 Agent 推理 |
| Hetzner | 运行 MCP、数据库和安全边界 | 不成为会计账本或通用文件仓库 |

### 7.3 身份与连接模型

每次 MCP 请求必须由 Work 服务端提供可验证身份，至少包含：

- `actor_id`：当前用户；
- `workspace_id`：当前团队或客户空间；
- `agent_id`：执行调用的 Agent；
- `roles` / `policy`：该用户与 Agent 的权限；
- `connection_id`：服务端选择的 Xero 连接。

调用方不能通过普通工具参数自行声明 actor、tenant 或权限。MCP 根据服务端连接记录解析 Xero Tenant。一个连接可绑定一个用户或工作区，并明确列出可使用它的成员。多 Xero 组织授权时必须让用户明确选择，不能默认取第一个组织。

### 7.4 Agent 自主性分级

| 等级 | 动作 | 默认策略 |
|---|---|---|
| L0 | 连接状态、组织、能力和权限查询 | Agent 可自主执行 |
| L1 | 联系人、历史发票/账单、附件元数据、余额和报表读取 | Agent 可自主执行，结果有界并审计 |
| L2 | 历史分析、材料比对、匹配建议、草稿预览 | Agent 可自主执行，必须说明依据和不确定性 |
| L3 | 创建或更新 `DRAFT`、上传附件到草稿 | 可由授权 Agent 执行；需来源绑定、防重复和回读 |
| L4 | `AUTHORISE`、Credit Note 分配、付款、Void、Delete、Manual Journal | 必须经明确人工确认；V1 除既有受控 Authorise 外默认不开放 |
| L5 | 批量不可逆动作、银行付款、税务申报 | V1 禁止 |

“Agent 可以自主做到大部分事情”在 V1 中指 L0-L3，而不是取消 L4-L5 的责任边界。

### 7.5 V1 工具能力

#### 连接与发现

- 查看连接状态、可用组织、当前组织和已授权能力；
- 发起 OAuth、选择组织、刷新连接、断开连接；
- 查询当前用户/Agent 对当前连接的动作权限。

#### 历史读取与分析基础

- 查询联系人，支持名称、邮箱和精确 ID；
- 列出并读取应付账单与应收发票，支持类型、联系人、日期、状态、搜索、排序和分页；
- 读取付款、Credit Note、历史记录和附件元数据；
- 读取 Trial Balance、Profit & Loss、Balance Sheet、Aged Payables 和 Aged Receivables；
- 返回标准化金额、币种、状态、到期/已付/已抵扣金额和 Xero 对象 ID。

所有过滤和排序由服务端白名单构造；不接受原始 Xero 查询语句。大结果必须分页，返回给 Agent 的行数和文本长度必须受限。

#### 材料与证据

- 接收 Work 已登记的材料引用、内容哈希、类型、时间和提取字段；
- 一个工作包支持多份材料，并保存字段级来源；
- 对金额、币种、日期、供应商、税额和订单号冲突进行显式提示；
- 原始文件仍由 Work 的文件能力管理；MCP 只保存必要引用和审计证据；
- 需要时把原始文件作为附件上传至对应 Xero 草稿，并回读附件状态。

#### 草稿与回读

- V1 保留供应商 Bill 草稿，并扩展销售 Invoice 草稿；
- 使用已有联系人、科目、税码和跟踪维度，不允许 Agent 凭空创造账务配置；
- 写入前返回完整预览：对象类型、联系人、日期、币种、明细、税、总额和来源；
- 每次写入包含稳定请求 ID 和内容哈希；相同请求重复调用返回原结果，内容变化则报冲突；
- 写入后按同一 Xero ID 回读并对比关键字段；结果不确定时停止，禁止盲目重试。

### 7.6 数据与审计

MCP 数据库只保存连接与控制面数据：

- 加密 OAuth Token、连接状态和 Tenant 映射；
- 用户、工作区、Agent 与连接授权关系；
- Source Bundle 的必要引用、哈希和字段证据；
- 工具调用意图、输入摘要、Xero 对象 ID、结果状态和错误；
- 草稿、审批、防重复和恢复状态。

数据库不复制完整总账。日志和审计中不得保存 OAuth Token、MCP 凭证或不必要的原始材料正文。

### 7.7 安全和可靠性要求

- Xero OAuth 使用最小权限，按功能显式声明缺失 scope；
- Token 静态加密、刷新串行化、撤销与断开可追踪；
- 可信 Work 身份替换固定 Demo Bearer 映射；禁止信任用户可伪造的身份 Header；
- 所有连接查询同时检查 actor、workspace、agent policy 和 connection binding；
- 写入策略按工作区、连接、动作和角色控制，同时保留紧急全局关闭；
- 对 Xero 限流、分页、Token 过期和暂时故障返回可恢复但不误写的错误；
- 每个工具有严格输入 Schema、结果上限、超时和审计；
- 不提供通用代理、任意 Endpoint 或任意查询工具。

### 7.8 V1 验收场景

1. **自助连接与隔离**：用户 A 在 Work 连接自己的 Xero、选择组织并断开；用户 B 无法看到或使用该连接。
2. **客户历史调查**：会计询问某客户过去 12 个月的销售发票、付款和逾期情况，Agent 能分页读取、汇总并链接到具体 Xero 记录。
3. **多材料分析**：用户加入发票、采购单和邮件；Agent 能指出一致字段、冲突字段、缺失字段及每项来源。
4. **会计匹配与草稿**：Agent 根据 Xero 现有历史建议供应商/客户、科目、税码和跟踪维度，用户接受后创建带来源的草稿并回读。
5. **审批与正式结果**：用户在 Work 审核同一草稿，批准后 Xero 状态变化；Agent 回读同一对象并解释账本影响。
6. **失败与重复安全**：重复请求不创建重复对象；权限不足、连接失效、Xero 超时、材料冲突和回读不一致均停止并给出明确下一步。

## 8. Release

### Phase 1：历史读取基础

- 增加应收/应付发票列表和详情；
- 支持联系人、日期、状态、搜索、排序和分页；
- 保持现有 Demo 工具向后兼容；
- 完成契约测试、边界测试和 Agent 结果大小限制。

退出条件：UAT 场景 2 的核心历史读取通过；不存在原始查询注入、Tenant 注入或无限结果。

### Phase 2：多用户连接与权限

- 引入可信 Work Principal；
- 建立 workspace/user/agent/connection 授权关系；
- 支持一个用户或工作区连接多个 Xero 组织并明确选择；
- 将全局 Demo 权限升级为分层动作策略。

退出条件：UAT 场景 1 通过，并完成跨用户、跨工作区、跨 Tenant 的负向安全测试。

### Phase 3：材料证据与草稿

- Source Bundle、多材料引用、字段来源和冲突处理；
- 联系人、科目、税码和跟踪维度候选匹配；
- 供应商 Bill 与销售 Invoice 草稿；
- 附件上传、来源绑定、防重复和精确回读。

退出条件：UAT 场景 3、4 和 6 通过。

### Phase 4：Agent2 会计用户闭环

- 在 Agent2 完成连接、权限说明、材料添加、预览、审批、回读和错误恢复体验；
- Agent 配置使用业务语言，隐藏 Tenant ID、InvoiceID、request_id 等技术细节；
- 在线完成会计人员视角的全流程验收并归档证据。

退出条件：六个 UAT 场景全部通过，安全审查、代码审查、回归测试和线上只读/受控写入验证均通过。

### Phase 5：V2 候选

- 多客户组织工作台与团队角色；
- Payment、Credit Note、银行数据只读和对账建议；
- 月结工作包、批量材料和批量草稿；
- Webhook 驱动的状态更新；
- 在相同连接与策略模型下评估 QuickBooks Provider。

任何 V2 能力必须沿用最小权限、来源证据、人工审批、防重复、精确回读和租户隔离，不得通过开放通用 Xero API 工具绕过这些控制。
